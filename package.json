{
  "name": "grav-storage",
  "version": "0.1.0",
  "private": true,
  "description": "Grav Storage — self-hosted, API-key-authenticated object storage for the GRAV platform.",
  "scripts": {
    "dev": "next dev -p 4000",
    "build": "next build",
    "start": "next start -p 4000",
    "check:mongo": "node scripts/check-mongo.mjs"
  },
  "dependencies": {
    "mongoose": "^8.9.0",
    "next": "^16.3.5",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "dotenv": "^17.0.0"
  }
}
